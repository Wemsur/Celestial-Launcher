import { Sequence, useCurrentFrame, useVideoConfig, interpolate } from "@remotion/react";export const TitleCard = ({ title, subtitle }) => {  const frame = useCurrentFrame();  const { fps } = useVideoConfig();  const opacity = interpolate(frame, [0, 10, 60 * 2], [0, 1, 1]);  const translateY = interpolate(frame, [0, 10, 60 * 2], [50, 0, 0]);  return (    <Sequence durationInFrames={60 * 3}>      <div style={{        width: "100%",        height: "100%",        display: "flex",        flexDirection: "column",        justifyContent: "center",        alignItems: "center",        backgroundColor: "#1a1a1a",        opacity,        transform: `translateY(${translateY}px)`,        fontFamily: "Inter, sans-serif",      }}>        <div style={{ fontSize: 96, color: "white", marginBottom: 24, textAlign: "center" }}>{title}</div>        {subtitle && (          <div style={{ fontSize: 48, color: "#aaa", fontFamily: "Inter, sans-serif" }}>{subtitle}</div>        )}      </div>    </Sequence>  );};export default TitleCard;
