import { ReactNode } from "react";export const AccountItem = ({ name, type, icon }) => {  const isPremium = type === "premium";  return (    <div style={{      display: "flex",      alignItems: "center",      padding: 24,      backgroundColor: isPremium ? "rgba(0,123,255,0.05)" : "rgba(108,117,125,0.05)",      border: isPremium ? "1px solid #007bff" : "1px solid #6c757d",      borderRadius: 12,      marginBottom: 16,    }}>      <div style={{ marginRight: 16 }}>{icon}</div>      <div style={{ flex: 1 }}>        <div style={{ fontSize: 20, fontWeight: "bold", color: "white", marginBottom: 4 }}>{name}</div>        <div style={{ fontSize: 14, color: isPremium ? "#007bff" : "#aaa" }}>{type === "offline" ? "ÀëÏßÕËºÅ" : "Modrinth Õı°æ"}</div>      </div>    </div>  );};export default AccountItem;
