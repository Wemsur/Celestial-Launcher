import { Sequence } from "@remotion/react";import { TitleCard } from "./TitleCard";import { AccountList } from "./AccountList";import { CreateAccountDemo } from "./CreateAccountDemo";import { OAuthLoginDemo } from "./OAuthLoginDemo";import { PasswordSetup } from "./PasswordSetup";import { TwoFactorSetup } from "./TwoFactorSetup";export const Video = () => {  return (    <div style={{ width: "100%", height: "100%", backgroundColor: "#1a1a1a", position: "relative" }}>      {/* Opening title (0-3 seconds) */}      <Sequence durationInFrames={60 * 3}>        <TitleCard title="Celestial Launcher" subtitle="账户管理系统" />      </Sequence>      {/* Account List Introduction (3-8 seconds) */}      <Sequence startOffsetInFrames={60 * 3} durationInFrames={60 * 5}>        <h2 style={{ fontSize: 64, color: "white", textAlign: "center", marginBottom: "48px", fontFamily: "Inter, sans-serif" }}>账户管理</h2>        <AccountList />      </Sequence>      {/* Create Offline Account Demo (8-14 seconds) */}      <Sequence startOffsetInFrames={60 * 8} durationInFrames={60 * 6}>        <CreateAccountDemo />      </Sequence>      {/* OAuth Login Demo (14-20 seconds) */}      <Sequence startOffsetInFrames={60 * 14} durationInFrames={60 * 6}>        <OAuthLoginDemo />      </Sequence>      {/* Password & Security Features (20-28 seconds) */}      <Sequence startOffsetInFrames={60 * 20} durationInFrames={60 * 8}>        <PasswordSetup />        <TwoFactorSetup />      </Sequence>    </div>  );};export default Video;
